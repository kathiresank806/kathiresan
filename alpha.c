#include<stdio.h>
#include<conio.h>
main()
{
	clrscr();
	char ch;
	printf("Enter a character: ");
	scanf("%c",&ch);
	if
	{
		printf("%c is an alphabet",ch);
	}
	else
	{
		printf("%c is not an alphabet",ch);
	}
	getch();
}
